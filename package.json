{
  "name": "lagom-game-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && butler push dist quackqack/ld57:html5"
  },
  "dependencies": {
    "lagom-engine": "^1.2.1",
    "webfontloader": "^1.6.28",
    "pixi-filters": "^4.2.0"
  },
  "devDependencies": {
    "@types/webfontloader": "^1.6.34",
    "typescript": "^5.6.2",
    "vite": "^5.4.7"
  }
}
